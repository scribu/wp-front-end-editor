#!/bin/bash

# usage: ./bin/build-aloha wpSaveCancel
#    or: ./bin/build-aloha wpSaveCancel -w

plugin=$1

dir=aloha-plugins/$plugin/lib/

mkdir -p $dir

coffee -b $2 -o $dir/ coffee/aloha/$plugin-plugin
